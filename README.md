# KotakbasAI 🧊

**KotakbasAI** — современный чат-бот с искусственным интеллектом и панелью администратора для управления настройками и скрытого ответа пользователям.

## ✨ Особенности

### Для пользователей
- 🎨 **Современный тёмный дизайн** — уникальная чёрная тема с мятными акцентами
- 💬 **Умный чат** — общение с ИИ на русском языке
- 📱 **Адаптивный интерфейс** — работает на всех устройствах
- ⚡ **Мгновенные ответы** — оптимизированная загрузка сообщений
- 📜 **История чатов** — все диалоги сохраняются

### Для администраторов
- 🔐 **Защищённая панель** — вход по паролю
- ⚙️ **Гибкие настройки** — редактирование системного промта, выбор модели, лимиты токенов
- 👁️ **Просмотр всех чатов** — полная история переписки пользователей
- 🎭 **Скрытые ответы** — возможность отвечать от имени ИИ (пользователь не узнает)
- 🗑️ **Управление чатами** — удаление нежелательных диалогов

## 🛠 Технологический стек

| Компонент | Технология |
|-----------|------------|
| **Frontend** | React 18 + Vite |
| **Стили** | CSS3 (кастомный дизайн) |
| **Иконки** | React Icons (Feather) |
| **Markdown** | React Markdown + remark-gfm |
| **Backend** | Node.js + Express |
| **База данных** | MongoDB Atlas |
| **ИИ** | OpenRouter API (бесплатные модели) |

## 📦 Установка

### 1. Клонирование и зависимости
```bash
cd KotakbasAI
npm install
```

### 2. Настройка окружения
Создайте файл `.env` в корне проекта:

```env
# Порт сервера
PORT=5000

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/kotakbasai?retryWrites=true&w=majority

# OpenRouter API ключ
AI_API_KEY=sk-or-v1-your-api-key-here

# OpenRouter API URL
AI_API_URL=https://openrouter.ai/api/v1/chat/completions

# Модель по умолчанию (бесплатная)
AI_MODEL=meta-llama/llama-3.2-3b-instruct:free
```

### 3. Запуск MongoDB (если локально)
```bash
# Windows (служба)
net start MongoDB

# Или через mongod
mongod --config "C:\Program Files\MongoDB\Server\5.0\bin\mongod.cfg"
```

### 4. Запуск сервера разработки
```bash
npm run dev
```

Сервер запустится на:
- **Frontend:** http://localhost:3000
- **Backend:** http://localhost:5000

## 📚 Использование

### Пользовательский чат
1. Откройте http://localhost:3000
2. Начните писать сообщение в поле ввода
3. Нажмите **Отправить** или Enter
4. Получите ответ от KotakbasAI

### Панель администратора
1. Нажмите **⚙️ Панель администратора** в боковой панели
2. Введите пароль: `Жопа`
3. Перейдите на вкладку **Настройки** или **Все чаты**

#### Настройки
- **Системный промт** — определяет поведение и личность ИИ
- **Модель ИИ** — выбор модели OpenRouter (рекомендуется `:free` для бесплатных)
- **Максимум токенов** — ограничение длины ответа (по умолчанию 4000)

#### Управление чатами
- Просмотр всех чатов пользователей
- Чтение полной истории переписки
- Отправка ответов от имени ИИ
- Удаление чатов

## 🔌 API Endpoints

### Публичные (чат)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/api/chat` | Отправить сообщение, получить ответ ИИ |
| `GET` | `/api/chat/:id` | Получить историю чата |
| `GET` | `/api/chat/user/:userId` | Получить все чаты пользователя |
| `GET` | `/api/chat` | Получить последние чаты |

### Административные

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/admin/settings` | Получить настройки |
| `PUT` | `/api/admin/settings` | Обновить настройки |
| `GET` | `/api/admin/chats` | Получить все чаты |
| `GET` | `/api/admin/chats/:id` | Получить детали чата |
| `POST` | `/api/admin/chats/:id/reply` | Ответить от имени ИИ |
| `DELETE` | `/api/admin/chats/:id` | Удалить чат |

**Заголовок для админ-запросов:**
```
x-admin-password: Жопа
```

### Пример запроса

```bash
# Отправка сообщения
curl -X POST http://localhost:5000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет!", "userId": "user_123"}'

# Получение настроек (админ)
curl -X GET http://localhost:5000/api/admin/settings \
  -H "x-admin-password: Жопа"
```

## 🎨 Дизайн-система

### Цветовая палитра
```css
--bg-primary: #0f0f0f      /* Основной фон */
--bg-secondary: #1a1a1a    /* Вторичный фон */
--bg-tertiary: #151515     /* Третичный фон */
--accent: #00d9a5          /* Акцентный (мятный) */
--accent-dark: #00b894     /* Тёмный акцент */
--text-primary: #ffffff    /* Основной текст */
--text-secondary: #e0e0e0  /* Вторичный текст */
--text-muted: #888888      /* Приглушённый текст */
--border: #2a2a2a          /* Границы */
```

### Шрифты
- **Основной:** Inter, -apple-system, BlinkMacSystemFont, Segoe UI
- **Моноширинный:** JetBrains Mono, Consolas, Monaco

### Иконки
Библиотека: **React Icons** (Feather Icons)
- FiSend, FiPlus, FiMessageSquare, FiSettings, FiLock
- FiLogOut, FiHome, FiTrash2, FiSave, FiEdit2, FiCheck

## 🔐 Безопасность

- Пароль администратора хранится в `.env` и на клиенте (localStorage)
- Для продакшена рекомендуется:
  - Использовать JWT-аутентификацию
  - Хэшировать пароли (bcrypt)
  - Добавить rate limiting
  - Настроить CORS для конкретного домена

## 📁 Структура проекта

```
KotakbasAI/
├── server/
│   ├── index.js              # Точка входа Express
│   ├── models/
│   │   ├── Chat.js           # Модель чата
│   │   └── Settings.js       # Модель настроек
│   ├── routes/
│   │   ├── chat.js           # Маршруты чата
│   │   └── admin.js          # Маршруты админки
│   └── services/
│       └── ai.js             # Сервис ИИ (OpenRouter)
├── client/
│   ├── src/
│   │   ├── App.jsx           # Корневой компонент
│   │   ├── main.jsx          # Точка входа React
│   │   ├── index.css         # Глобальные стили
│   │   └── pages/
│   │       ├── ChatPage.jsx      # Страница чата
│   │       └── AdminPanel.jsx    # Панель администратора
│   ├── index.html
│   └── vite.config.js
├── .env                      # Переменные окружения
├── .gitignore
├── package.json
├── README.md                 # Этот файл
├── DEVELOPMENT.md            # Руководство для разработчиков
└── CONTEXT.md                # Контекст проекта
```

## 🚀 Сборка для продакшена

```bash
# Сборка frontend
npm run build

# Запуск production сервера
npm start
```

## 📝 Лицензия

MIT License — свободное использование с указанием авторства.

## 🤝 Поддержка

Для вопросов и предложений создайте issue в репозитории проекта.

---

**KotakbasAI** © 2025. Создано с ❤️ для удобного общения с ИИ.
