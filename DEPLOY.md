# 🚀 Руководство по деплою KotakbasAI

Полное руководство по развёртыванию приложения на **бесплатных тарифах** Render ИЛИ Vercel.

---

## 💰 Важно: Бесплатные тарифы

Все сервисы работают на **FREE** планах:

| Сервис | Тариф | Что включено | Ограничения |
|--------|-------|--------------|-------------|
| **GitHub** | Free | Безлимитные репо | — |
| **MongoDB Atlas** | M0 Sandbox | 512 MB, общий доступ | Нет бэкапов |
| **Render** | Free | 750 часов/месяц, 0.1 CPU, 512 MB RAM | **Засыпает через 15 мин бездействия** |
| **Vercel** | Hobby | 100 GB-hours, 100 GB bandwidth | Лимит трафика |
| **OpenRouter** | Free Models | Бесплатные модели | Rate limits |

**⚠️ Важно знать:**

1. **Render Free** — сервис "засыпает" через 15 минут бездействия. Первый запрос после простоя обрабатывается **30-60 секунд** (холодный старт).
2. **Vercel Hobby** — нет холодного старта, но есть лимит трафика (100 GB/месяц).
3. **MongoDB Atlas Free** — 512 MB хватит на ~50,000+ сообщений.
4. **OpenRouter Free** — бесплатные модели могут быть rate-limited.

**Рекомендация:** Для тестов и демо — отлично подходит. Для продакшена рассмотрите платные тарифы.

---

## 📋 Содержание

1. [Что понадобится](#что-понадобится)
2. [Подготовка проекта](#подготовка-проекта)
3. [Вариант 1: Деплой на Render (Full Stack)](#вариант-1-деплой-на-render-full-stack)
4. [Вариант 2: Деплой на Vercel (Full Stack)](#вариант-2-деплой-на-vercel-full-stack)
5. [Сравнение платформ](#сравнение-платформ)
6. [Проверка работы](#проверка-работы)
7. [Частые проблемы](#частые-проблемы)
8. [Информация для ИИ-агента](#информация-для-ии-агента)

---

## 🛠 Что понадобится

| Сервис | Зачем | Стоимость |
|--------|-------|-----------|
| **GitHub** | Хранение кода | Бесплатно |
| **MongoDB Atlas** | База данных | Бесплатно (512 MB) |
| **Render** | Full Stack (Node.js + React) | Бесплатно |
| **Vercel** | Full Stack (Serverless Functions + Static) | Бесплатно |
| **OpenRouter** | ИИ API | Бесплатные модели |

**Время на деплой:** ~30-45 минут на каждую платформу

---

## 📦 Подготовка проекта

### Шаг 1: Создайте репозиторий на GitHub

```bash
# В папке проекта
git init
git add .
git commit -m "Initial commit: KotakbasAI v2.0"
git branch -M main
git remote add origin https://github.com/ВАШ_НИК/kotakbasai.git
git push -u origin main
```

### Шаг 2: Проверьте `.gitignore`

Убедитесь, что `.env` НЕ попадает в репозиторий:

```
node_modules/
dist/
.env
*.log
.DS_Store
```

---

## 🎯 Вариант 1: Деплой на Render (Full Stack)

### Что будет

- **Backend:** Node.js + Express (API)
- **Frontend:** Vite build (статические файлы)
- **URL:** `https://kotakbasai.onrender.com`

### Шаг 1: Создайте аккаунт на Render

1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub

### Шаг 2: Создайте Web Service

1. Нажмите **New +** → **Web Service**
2. Выберите ваш репозиторий `kotakbasai`
3. Настройте:

| Поле | Значение |
|------|----------|
| **Name** | `kotakbasai` |
| **Region** | Frankfurt (Germany) |
| **Branch** | `main` |
| **Root Directory** | (оставьте пустым) |
| **Runtime** | `Node` |
| **Build Command** | `npm install && npm run build` |
| **Start Command** | `node server/index.js` |
| **Instance Type** | `Free` |

### Шаг 3: Добавьте переменные окружения

Нажмите **Advanced** → **Add Environment Variable**:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `MONGODB_URI` | `mongodb+srv://rex_corp:j52zsm%25Z@kotakbasai.a57iqj7.mongodb.net/kotakbasai?retryWrites=true&w=majority` |
| `AI_API_KEY` | `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` |
| `AI_API_URL` | `https://openrouter.ai/api/v1/chat/completions` |
| `AI_MODEL` | `meta-llama/llama-3.2-3b-instruct:free` |
| `ADMIN_PASSWORD` | `Жопа` |

⚠️ **Важно:** Для `MONGODB_URI` используйте URL-encoding для пароля:
- `#` → `%23`
- `@` → `%40`
- `%` → `%25`

### Шаг 4: Задеплойте

1. Нажмите **Create Web Service**
2. Подождите 5-7 минут (Build → Deploy)
3. Скопируйте URL сервиса, например: `https://kotakbasai.onrender.com`

### Шаг 5: Проверьте

Откройте `https://kotakbasai.onrender.com` в браузере.

---

## 🎯 Вариант 2: Деплой на Vercel (Full Stack)

### Что будет

- **Backend:** Vercel Serverless Functions (`api/`)
- **Frontend:** Vite build (статические файлы)
- **URL:** `https://kotakbasai.vercel.app`

### Шаг 1: Подготовьте проект для Vercel

Проект уже готов! Файлы:
- `api/index.js` — chat API
- `api/admin.js` — admin API
- `vercel.json` — конфигурация

### Шаг 2: Создайте аккаунт на Vercel

1. Перейдите на https://vercel.com
2. Зарегистрируйтесь через GitHub

### Шаг 3: Импортируйте проект

1. Нажмите **Add New...** → **Project**
2. Найдите репозиторий `kotakbasai`
3. Нажмите **Import**

### Шаг 4: Настройте проект

| Поле | Значение |
|------|----------|
| **Framework Preset** | `Vite` |
| **Root Directory** | (оставьте пустым) |
| **Build Command** | `npm run build` |
| **Output Directory** | `client/dist` |
| **Install Command** | `npm install` |

### Шаг 5: Добавьте переменные окружения

Раскройте **Environment Variables** → **Add**:

| Key | Value |
|-----|-------|
| `MONGODB_URI` | `mongodb+srv://rex_corp:j52zsm%25Z@kotakbasai.a57iqj7.mongodb.net/kotakbasai?retryWrites=true&w=majority` |
| `AI_API_KEY` | `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` |
| `AI_API_URL` | `https://openrouter.ai/api/v1/chat/completions` |
| `AI_MODEL` | `meta-llama/llama-3.2-3b-instruct:free` |
| `ADMIN_PASSWORD` | `Жопа` |

### Шаг 6: Задеплойте

1. Нажмите **Deploy**
2. Подождите 3-5 минут
3. Скопируйте URL, например: `https://kotakbasai.vercel.app`

### Шаг 7: Проверьте

Откройте `https://kotakbasai.vercel.app` в браузере.

---

## 📊 Сравнение платформ (Free тарифы)

| Критерий | Render Free | Vercel Hobby |
|----------|-------------|--------------|
| **Холодный старт** | ⚠️ 30-60 сек (после 15 мин простоя) | ✅ Нет |
| **Время работы** | 750 часов/месяц (24/7 = 744 часа) | Безлимит (по запросам) |
| **Трафик** | 100 GB/месяц | 100 GB/месяц |
| **RAM** | 512 MB | 1024 MB (функции) |
| **CPU** | 0.1 CPU | Shared |
| **Сложность** | ⭐ Проще (один сервис) | ⭐⭐ Сложнее (Serverless) |
| **Для тестов** | ✅ Отлично | ✅ Отлично |
| **Для продакшена** | ⚠️ Холодный старт | ✅ Лучше |

**Вывод:** 
- **Render** — проще в настройке, но холодный старт может раздражать пользователей
- **Vercel** — быстрее, но требует правильной настройки serverless функций

**Рекомендация для начала:** Начните с **Render** для простоты. Если холодный старт станет проблемой — перейдите на Vercel.

---

## ✅ Проверка работы

### Чеклист

- [ ] Сайт открывается без ошибок
- [ ] Можно отправить сообщение ИИ
- [ ] ИИ отвечает (не fallback)
- [ ] Чаты сохраняются в БД
- [ ] Админ-панель открывается
- [ ] Пароль администратора работает
- [ ] Настройки сохраняются
- [ ] Ответы админа работают

### Тестовый сценарий

1. Откройте сайт
2. Напишите: "Привет, как тебя зовут?"
3. Должен прийти ответ от KotakbasAI на русском
4. Откройте админ-панель (пароль: `Жопа`)
5. Проверьте, что чат появился в списке
6. Отправьте ответ от админа
7. Проверьте в чате, что ответ появился

---

## 🐛 Частые проблемы (Free тарифы)

### 1. Render: Долгий ответ после простоя

**Симптом:** Первый запрос после 15+ минут простоя обрабатывается 30-60 секунд

**Причина:** Render Free "засыпает" для экономии ресурсов

**Решение:**
- ⭐ **Платное:** Upgrade к Render Starter ($7/месяц) — нет холодного старта
- ⭐ **Бесплатное:** Используйте UptimeRobot (https://uptimerobot.com) для пинга каждые 5 минут
  - Создайте мониторинг: HTTP(s) → `https://kotakbasai.onrender.com/health`
  - Интервал: 5 минут
  - ⚠️ Это может нарушать ToS Render — используйте на свой страх и риск

### 2. MongoDB: Закончилось место

**Симптом:** `MongoServerError: Quota exceeded`

**Причина:** Превышен лимит 512 MB

**Решение:**
- Удалите старые чаты через админ-панель
- Очистите коллекцию `chats` с большими сообщениями
- 512 MB = ~50,000+ обычных сообщений

### 3. OpenRouter: Rate Limit

**Симптом:** ИИ возвращает fallback сообщение

**Причина:** Бесплатные модели имеют лимиты на запросы

**Решение:**
- Подождите 1-2 минуты и попробуйте снова
- Используйте другую бесплатную модель в настройках
- Добавьте свой API ключ (платный)

### 4. Vercel: Превышен лимит трафика

**Симптом:** `403 Forbidden` или `Deployment disabled`

**Причина:** Превышен лимит 100 GB/месяц

**Решение:**
- Дождитесь следующего месяца (сброс 1 числа)
- Уменьшите размер статики (оптимизируйте изображения)
- Upgrade к Pro ($20/месяц)

### 5. Vercel: API не работает (500 error)

**Симптом:** `500 Internal Server Error` при отправке сообщения

**Причина:** Не настроены Environment Variables или ошибка в serverless функции

**Решение:**
1. Проверьте **Vercel Dashboard** → **Settings** → **Environment Variables**
2. Убедитесь что все переменные добавлены (см. выше)
3. Проверьте **Function Logs** для деталей ошибки
4. Сделайте **Redeploy** после добавления переменных

### 6. Render: Build failed

**Ошибка:** `Build failed` или `Crash`

**Решение:**
```bash
# Проверьте логи в Render Dashboard → Logs
# Частые причины:
# - Нет package.json в корне
# - Неправильный startCommand
# - Ошибка в .env переменных
```

### 7. MongoDB не подключается

**Ошибка:** `MongoServerError: Authentication failed`

**Решение:**
- Проверьте пароль в `MONGODB_URI`
- Убедитесь, что пароль URL-encoded
- Проверьте Network Access (0.0.0.0/0)

### 8. Админка не работает (кириллица в пароле)

**Ошибка:** `Неверный пароль` при правильном вводе

**Решение:**
Проблема в кодировке кириллицы. Используйте латинский пароль:
```javascript
// api/admin.js
const ADMIN_PASSWORD = process.env.ADMIN_PASSWORD || 'Zhopa123!';
```

---

## 💡 Советы для бесплатных тарифов

### Оптимизация Render Free

1. **Уменьшите время сборки:**
   - Кэшируйте `node_modules` (Render делает это автоматически)
   - Удалите лишние зависимости из `package.json`

2. **Предотвратите краши:**
   - Следите за использованием RAM (лимит 512 MB)
   - Логи пишутся в `/var/log` — могут занять место

3. **Мониторинг:**
   - Проверьте Dashboard → Metrics
   - Настройте алерты на Email

### Оптимизация Vercel Hobby

1. **Уменьшите размер билда:**
   - Удалите неиспользуемые зависимости
   - Используйте динамические импорты для больших библиотек

2. **Оптимизируйте статику:**
   - Сжимайте изображения (TinyPNG)
   - Используйте современный формат WebP

3. **Кэширование:**
   - Vercel автоматически кэширует статику
   - Используйте `Cache-Control` заголовки

### MongoDB Atlas Free

1. **Экономия места:**
   - Регулярно удаляйте старые чаты
   - Ограничьте `maxTokens` для коротких ответов

2. **Индексы:**
   - Создайте индексы на `userId`, `createdAt`
   - Ускорит поиск чатов

### OpenRouter Free Models

1. **Выбор модели:**
   - `meta-llama/llama-3.2-3b-instruct:free` — быстрая, но простая
   - `nousresearch/hermes-3-llama-3.1-405b:free` — умнее, но медленнее

2. **Fallback логика:**
   - В коде уже есть переключение между моделями
   - При rate limit пробуем следующую

---

## 📊 Информация для ИИ-агента

### Архитектура для Render (Full Stack)

```
┌─────────────────────────────────────┐
│         Render Web Service          │
│  https://kotakbasai.onrender.com    │
│                                     │
│  ┌──────────────┐  ┌─────────────┐ │
│  │   Express    │  │   Vite      │ │
│  │   (API)      │─▶│   (Static)  │ │
│  │  Node.js     │  │   React     │ │
│  └──────────────┘  └─────────────┘ │
│           │                         │
│           ▼                         │
│  ┌─────────────────┐                │
│  │   MongoDB       │                │
│  │   (Atlas)       │                │
│  └─────────────────┘                │
│           │                         │
│           ▼                         │
│  ┌─────────────────┐                │
│  │   OpenRouter    │                │
│  │   (AI API)      │                │
│  └─────────────────┘                │
└─────────────────────────────────────┘
```

### Архитектура для Vercel (Full Stack)

```
┌─────────────────────────────────────┐
│        Vercel Deployment            │
│  https://kotakbasai.vercel.app      │
│                                     │
│  ┌──────────────┐  ┌─────────────┐ │
│  │   Serverless │  │   Vite      │ │
│  │   Functions  │─▶│   (Static)  │ │
│  │   (api/)     │  │   React     │ │
│  └──────────────┘  └─────────────┘ │
│           │                         │
│           ▼                         │
│  ┌─────────────────┐                │
│  │   MongoDB       │                │
│  │   (Atlas)       │                │
│  └─────────────────┘                │
│           │                         │
│           ▼                         │
│  ┌─────────────────┐                │
│  │   OpenRouter    │                │
│  │   (AI API)      │                │
│  └─────────────────┘                │
└─────────────────────────────────────┘
```

### Критические переменные (обе платформы)

```env
NODE_ENV=production
MONGODB_URI=mongodb+srv://rex_corp:j52zsm%25Z@kotakbasai.a57iqj7.mongodb.net/kotakbasai?retryWrites=true&w=majority
AI_API_KEY=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
AI_API_URL=https://openrouter.ai/api/v1/chat/completions
AI_MODEL=meta-llama/llama-3.2-3b-instruct:free
ADMIN_PASSWORD=Жопа
```

### Структура файлов для деплоя

```
KotakbasAI/
├── api/
│   ├── index.js          # Vercel: Chat API
│   └── admin.js          # Vercel: Admin API
├── server/
│   ├── index.js          # Render: Express server
│   ├── models/
│   ├── routes/
│   └── services/
├── client/
│   ├── src/
│   └── dist/             # Build output
├── vercel.json           # Vercel конфигурация
├── render.yaml           # Render конфигурация
└── package.json
```

### Команды для деплоя

**Render:**
```bash
git push origin main
# Автоматический деплой
```

**Vercel:**
```bash
git push origin main
# Автоматический деплой
# Или через CLI:
vercel --prod
```

### Обновление деплоя

**Обе платформы:**
```bash
git push origin main
```

Автоматически запускается CI/CD пайплайн.

### Rollback

**Render:**
1. Dashboard → Deployments
2. Выберите предыдущую версию
3. Promote to Production

**Vercel:**
1. Dashboard → Deployments
2. Нажмите на предыдущий деплой
3. Promote to Production

---

## 📝 Чеклист перед деплоем

- [ ] Код в GitHub (main branch)
- [ ] `.env` в `.gitignore`
- [ ] MongoDB Atlas кластер создан
- [ ] Network Access: 0.0.0.0/0
- [ ] Render аккаунт создан
- [ ] Vercel аккаунт создан
- [ ] Все переменные записаны
- [ ] URLs записаны

---

## 🎯 Быстрый старт

```bash
# 1. GitHub
git push -u origin main

# 2. MongoDB Atlas
# Создать кластер → User → Network Access → Connect String

# 3A. Render (Full Stack)
# New Web Service → GitHub repo → Add env vars → Deploy
# URL: https://kotakbasai.onrender.com

# 3B. Vercel (Full Stack)
# Import Project → Add env vars → Deploy
# URL: https://kotakbasai.vercel.app

# 4. Test
# Open URLs → Chat → Admin Panel
```

---

## 📞 Поддержка

Если что-то пошло не так:

1. Проверьте логи (Render/Vercel Dashboard)
2. Проверьте переменные окружения
3. Проверьте подключение к MongoDB
4. Создайте issue в репозитории

---

**Последнее обновление:** Февраль 2025  
**Версия документа:** 2.1.0 (Vercel Serverless)  
**Статус:** Production Ready
