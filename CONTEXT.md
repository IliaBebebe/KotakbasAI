# Контекст проекта KotakbasAI 🧊

Этот документ сохраняет контекст проекта для будущих сессий с ИИ-ассистентом.

---

## 📌 Основная информация

**Название:** KotakbasAI  
**Версия:** 2.0.0 (Full Stack)  
**Дата создания:** Февраль 2025  
**Язык интерфейса:** Русский  
**Пароль администратора:** `Жопа` (или `Zhopa123!` для продакшена)

---

## 🎯 Назначение проекта

KotakbasAI — это чат-приложение с ИИ, которое позволяет:
1. Пользователям общаться с ИИ-ассистентом на русском языке
2. Администраторам управлять настройками ИИ (системный промт, модель)
3. Администраторам просматривать все чаты пользователей
4. Администраторам отвечать пользователям от имени ИИ (скрыто)

**Архитектура:** Full Stack (frontend + backend в одном сервисе)

---

## 🏁 Ключевые решения и требования

### Дизайн
- **Тёмная тема** (#1a1c23 сине-серый) — НЕ чёрный, НЕ фиолетовый
- **Акцентный цвет:** мятный/бирюзовый (#00d9a5)
- **Уникальный стиль** — минимализм, строгие линии
- **Иконки:** React Icons (Feather)

### Функциональность
- **Поле ввода активно** во время ответа ИИ (кнопка неактивна)
- **Авто-скролл вниз** — новые сообщения всегда видны
- **Все чаты сохраняются** — каждый пользователь имеет userId
- **Markdown поддержка** — react-markdown + remark-gfm
- **Нет ограничения токенов** — max_tokens: 4000 (было 1000)

### Деплой
- **Render:** Full Stack (Node.js + Vite build) — **FREE** (засыпает через 15 мин)
- **Vercel:** Full Stack (Serverless Functions + Static) — **FREE** (100 GB/месяц)
- **Обе платформы** могут разворачивать полный стек
- **server/index.js:** serving статики из `client/dist`
- **MongoDB Atlas:** FREE (512 MB, ~50K сообщений)
- **OpenRouter:** Бесплатные модели (rate-limited)

### ИИ-модель
- **Провайдер:** OpenRouter (бесплатные модели)
- **Модель по умолчанию:** `meta-llama/llama-3.2-3b-instruct:free`
- **Fallback система** — если модель rate-limited, пробуем следующую
- **Системный промт:** KotakbasAI, модель Kotakbas-1.0-32B, русский язык

---

## 📁 Структура файлов

```
KotakbasAI/
├── server/
│   ├── index.js              # Express + MongoDB подключение
│   ├── models/
│   │   ├── Chat.js           # Схема чата (userId, messages[])
│   │   └── Settings.js       # Схема настроек (systemPrompt, aiModel)
│   ├── routes/
│   │   ├── chat.js           # POST /api/chat, GET /api/chat/:id
│   │   └── admin.js          # Админские эндпоинты с паролем
│   └── services/
│       └── ai.js             # OpenRouter API + fallback логика
├── client/
│   ├── src/
│   │   ├── App.jsx           # React Router
│   │   ├── main.jsx          # Точка входа
│   │   ├── index.css         # Глобальные стили (чёрная тема)
│   │   └── pages/
│   │       ├── ChatPage.jsx      # Чат с ИИ
│   │       └── AdminPanel.jsx    # Админка с паролем
│   └── index.html
├── .env                      # MONGODB_URI, AI_API_KEY, AI_MODEL
├── package.json
├── README.md                 # Основная документация
├── DEVELOPMENT.md            # Для разработчиков
└── CONTEXT.md                # Этот файл
```

---

## 🔑 Ключевые значения

### .env (текущие)
```env
PORT=5000
MONGODB_URI=mongodb+srv://rex_corp:j52zsm%25Z@kotakbasai.a57iqj7.mongodb.net/kotakbasai
AI_API_KEY=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
AI_API_URL=https://openrouter.ai/api/v1/chat/completions
AI_MODEL=meta-llama/llama-3.2-3b-instruct:free
```

### Системный промт (текущий)
```
Вы - полезный ассистент с искусственным интеллектом по имени KotakbasAI. 
Будьте дружелюбны, лаконичны и услужливы. 
ВАЖНО: Ты не та ИИ-модель, которая была изначально. 
Ты - KotakbasAI, модель Kotakbas-1.0-32B. 
Общайся на русском языке, если пользователь не попросит общаться на ином. 
Важно быть более услужливым и в меру лаконичным, нежели дружелюбным. 
Общайся с уважением.
```

### Список бесплатных моделей
```javascript
[
  'meta-llama/llama-3.2-3b-instruct:free',
  'nousresearch/hermes-3-llama-3.1-405b:free',
  'stepfun/step-3.5-flash:free',
  'upstage/solar-pro-3:free',
  'arcee-ai/trinity-large-preview:free'
]
```

---

## 🚧 Известные проблемы

1. **Rate limiting** — бесплатные модели часто rate-limited
   - Решение: fallback на другие модели в `ai.js`

2. **Пароль в клиентском коде** — небезопасно для продакшена
   - Решение: JWT-аутентификация с серверной сессией

3. **MongoDB Atlas** — требует настройки IP whitelist
   - Решение: добавить 0.0.0.0/0 для доступа отовсюду

---

## 📝 История изменений

### v1.0.0 (Февраль 2025)
- ✅ Чёрный дизайн (не фиолетовый)
- ✅ React Icons (Feather)
- ✅ Русский интерфейс полностью
- ✅ Авто-скролл к новым сообщениям
- ✅ Поле ввода активно во время ответа ИИ
- ✅ Увеличен лимит токенов до 4000
- ✅ Пароль на админку: "Жопа"
- ✅ Markdown поддержка (таблицы, код, списки)
- ✅ Сохранение всех чатов
- ✅ Fallback система для ИИ-моделей

---

## 🎯 Для будущей сессии

Если вы продолжаете работу над проектом:

1. **Прочитайте этот файл** для понимания контекста
2. **Проверьте DEVELOPMENT.md** для технических деталей
3. **Запустите проект:**
   ```bash
   npm run dev
   ```
4. **Проверьте работу:**
   - Чат: http://localhost:3000
   - Админка: http://localhost:3000/admin (пароль: Жопа)

5. **Для изменений:**
   - Дизайн → `client/src/index.css`
   - Логика чата → `client/src/pages/ChatPage.jsx`
   - Админка → `client/src/pages/AdminPanel.jsx`
   - ИИ → `server/services/ai.js`
   - Модель данных → `server/models/*.js`

---

## 💡 Запомнить для следующих сессий

- Пользователь предпочитает **уникальный дизайн**, не стандартные градиенты
- **Русский язык** во всех интерфейсах и ответах ИИ
- **Функциональность важнее красоты** — но дизайн должен быть современным
- Все изменения должны быть **протестированы** перед завершением
- Сохраняйте **обратную совместимость** при изменениях API

---

**Создано для:** сохранения контекста между сессиями разработки  
**Обновлено:** Февраль 2025
